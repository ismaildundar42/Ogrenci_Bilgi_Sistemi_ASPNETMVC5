@model MvcOkulProject.Models.EntitiyFramework.tbl_dersler
@{
    ViewBag.Title = "DersGetir";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("DersGuncelle", "Default", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(m => m.DERSAD)
        @Html.TextBoxFor(m => m.DERSAD, new { @class = "form-control" })
    </div>
    @Html.HiddenFor(m=>m.DERSID)
    <div>
        <button class="btn btn-success">Güncelle</button>
    </div>
}

